# Cerberus Guard Bot — runtime (production) dependencies only.
# Dev/test tooling lives in requirements-dev.txt.
# Keep this list in sync with pyproject.toml [project.dependencies].
pydantic>=2.0.0
pydantic-settings>=2.0.0
structlog>=24.0.0
bcrypt>=4.0.0
cryptography>=41.0.0
